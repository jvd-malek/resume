# Bahman Sabz | Frontend Test

<img src="public/Logo-bahmansabz.webp" alt="Neynegar Logo" width="200"/>

پروژه فرانت‌اند تست بهمن سبز با **Next.js** و **React**.

🌐 **نسخه آنلاین:** [https://frontend-test-bahmansabz.vercel.app/](https://frontend-test-bahmansabz.vercel.app/)

---

## صفحات سایت

| مسیر | توضیح |
|------|--------|
| `/` | **صفحه اصلی (Home)** |
| `/dashboard` | **داشبورد** |
| `/products` | **لیست محصولات** |
| `/product/[id]` | **جزئیات هر محصول** |
| `/dropdown` | **کامپوننت Dropdown** |
| `/login` | **ورود / ثبت‌نام** |

---

## امکانات هر صفحه

### صفحه اصلی (`/`)
- نمایش لوگو و عنوان «بهمن سبز»
- اگر کاربر لاگین کرده باشد، پیام سلام با نام کاربر نمایش داده می‌شود
- کارت‌های لینک به: Dashboard، Products، Dropdown، Login
- هر کارت با هاور سبز و بوردر مشخص می‌شود

### داشبورد (`/dashboard`)
- **نیاز به لاگین:** در صورت نبودن کوکی کاربر، به `/login` ریدایرکت می‌شود
- **سایدبار:** دو تب «کاربران» و «محصولات» با لینک `?activeLink=users` و `?activeLink=products`
- **بخش کاربران:** لیست ۶ کاربر از [DummyJSON](https://dummyjson.com/users) (نام، ایمیل، id)
- **بخش محصولات:** گرید ۶ محصول با کامپوننت `ProductBox` (تصویر، عنوان، قیمت، لینک به صفحه جزئیات)
- داده‌ها با `revalidate: 3600` از API گرفته می‌شوند

### محصولات (`/products`)
- **فیلترها (GET):**
  - جستجو (`q`): نام محصول یا دسته‌بندی
  - دسته‌بندی (`category`): سلکت از دسته‌های موجود
  - بازه قیمت: `minPrice` و `maxPrice` (دلار)
  - دکمه «اعمال فیلتر» و «حذف فیلتر»
- **صفحه‌بندی:** ۱۲ آیتم در هر صفحه؛ لینک‌های قبلی/بعدی و شماره صفحات با حفظ فیلترها
- لیست محصولات از DummyJSON (حدود ۱۰۰ محصول)، فیلتر سمت سرور و صفحه‌بندی سمت سرور
- هر کارت محصول لینک به `/product/[id]` دارد

### جزئیات محصول (`/product/[id]`)
- دریافت تک محصول از `https://dummyjson.com/products/{id}`
- نمایش: تصویر اصلی، گالری تصاویر (در صورت وجود)، قیمت، دسته‌بندی، برند، امتیاز، موجودی، توضیحات
- دکمه «بازگشت به لیست محصولات»
- دکمه‌های فرضی: «افزودن به سبد خرید»، «افزودن به علاقه‌مندی‌ها»

### صفحه Dropdown (`/dropdown`)
- **۴ مثال** برای کامپوننت Dropdown:
  1. **انتخاب چندتایی با جستجو:** ۵ گزینه ساده، قابلیت جستجو، انتخاب همه
  2. **گروه‌بندی:** میوه‌ها و سبزیجات در گروه‌های جدا
  3. **مجازی‌سازی:** ۱۰۰۰ آیتم با `@tanstack/react-virtual` و `maxHeight={400}`
  4. **انتخاب تکی:** یک گزینه، با جستجو
- قابلیت‌های Dropdown: `searchable`, `multiple`, `selectAll`, `groupable`, `virtualized`

### ورود / ثبت‌نام (`/login`)
- **دو حالت:** ورود و ثبت‌نام (سوییچ با دکمه)
- **ثبت‌نام:** نام، نام کاربری، رمز عبور → ذخیره در کوکی و نمایش پیام موفقیت
- **ورود:** نام کاربری و رمز با داده ذخیره‌شده در کوکی مقایسه می‌شود؛ در صورت موفقیت ریدایرکت به `/`
- پیام‌های خطا و موفقیت به فارسی
- نمایش نام کاربری ذخیره‌شده در حالت ورود (راهنما)

---

## امکانات و تکنولوژی‌ها

- **فریم‌ورک:** Next.js 16 (App Router)
- **React:** نسخه ۱۹
- **زبان و استایل:** TypeScript، Tailwind CSS 4، فونت محلی **Baloo** (فارسی)
- **سئو و متادیتا:** متادیتای اختصاصی برای صفحه اصلی، داشبورد، محصولات، جزئیات محصول، Dropdown و ورود/ثبت‌نام (با استفاده از `metadata` و `generateMetadata` در App Router)
- **رابط کاربری:** کامپوننت‌های Headless UI، آیکون‌ها با `react-icons`
- **داده:**
  - فراخوانی API از [DummyJSON](https://dummyjson.com) برای کاربران و محصولات
  - کش و revalidate در `fetch`
  - کوکی برای ذخیره کاربر فرضی (ورود/ثبت‌نام)
- **کامپوننت‌های سفارشی:**
  - `Header`: لوگو + لینک‌های ناوبری + سلام به کاربر در صورت لاگین
  - `ProductBox`: کارت محصول با لینک به جزئیات
  - Dropdown با جستجو، چندانتخابی، گروه‌بندی و مجازی‌سازی (TanStack Virtual)
  - `AuthInput` برای فرم ورود/ثبت‌نام
- **دسترسی:** داشبورد فقط برای کاربر لاگین‌شده؛ چک کوکی در سرور (`getUserFromCookie`)
- **راست به چپ:** `dir="rtl"` و `lang="fa-IR"` در layout

---

## چت‌های هوش مصنوعی

- لیست چت‌هایی که برای توسعه این پروژه با هوش مصنوعی داشته‌اید، در فایل `chatbots.md` نگهداری می‌شود.

---

## اجرای پروژه

```bash
npm install
npm run dev
```

مرورگر: [http://localhost:3000](http://localhost:3000)

---

## اسکریپت‌ها

| دستور | کاربرد |
|--------|--------|
| `npm run dev` | سرور توسعه |
| `npm run build` | ساخت برای پروداکشن |
| `npm run start` | اجرای نسخه پروداکشن |
| `npm run lint` | اجرای ESLint |

---

## دیپلوی

پروژه روی [Vercel](https://vercel.com) دیپلوی شده و از طریق لینک زیر در دسترس است:

**https://frontend-test-bahmansabz.vercel.app/**

برای دیپلوی خودتان، ریپو را به Vercel متصل کنید یا از [مستندات دیپلوی Next.js](https://nextjs.org/docs/app/building-your-application/deploying) استفاده کنید.
